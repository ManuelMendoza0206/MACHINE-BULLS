# Auditoría de la suite E2E — Sprint 2, Tarea 2 (Leonardo, Asiento D)

**Fecha:** 17-sep-2026. Ejecutado en local (Node 24, `frontend/`), no solo leído del código.

---

## 1. Resultado de correr `npm run test:e2e` hoy

```
Running 4 tests using 3 workers
  ok 1 [chromium] navigation.spec.ts:5:5  desktop shows TopNav, mobile shows BottomTabBar
  ok 3 [chromium] theme.spec.ts:5:5       persisted dark theme, no FOUC on reload
  ok 4 [chromium] theme.spec.ts:21:5      the toggle cycles the theme on click
  ok 2 [chromium] navigation.spec.ts:25:5 keyboard-only: skip link, then navigate to Outfits

  4 passed (11.1s)
```

**Los 4 tests pasaron limpio, sin retries ni "flaky".** Una auditoría anterior (3-sep-2026,
sesión previa) había reportado 3 casos "flaky" (fallan al primer intento, pasan en retry) por
cold-start del dev server en un sandbox distinto. **No se reproduce hoy** — conclusión: fue un
problema del entorno de esa sesión (contención de recursos), no un defecto real de
`navigation.spec.ts`/`theme.spec.ts`. No requiere acción de código.

## 2. Inventario: cobertura E2E actual vs. los 4 flujos críticos

| Flujo crítico | Test E2E hoy | Motivo |
|---|---|---|
| Upload de prenda | ❌ Ninguno (scaffold `test.skip` añadido esta tarea, ver Tarea 3-4) | UI es stub (`wardrobe/page.tsx`), feature real es Sprint 3 (Huascar) |
| Recomendación de outfit | ❌ Ninguno (scaffold `test.skip`) | UI es stub (`outfits/page.tsx`), feature real es Sprint 4 (Manuel) |
| Ciclo VTON (submit→poll→resultado) | ❌ Ninguno (scaffold `test.skip`, 2 archivos) | UI es stub (`try-on/page.tsx`), feature real es Sprint 6 (Jaicel) |
| Visualización de resultado | ❌ Ninguno (scaffold `test.skip`) | Idem VTON |

**Confirmado: 0 de los 4 flujos críticos tienen cobertura E2E real hoy** — como se esperaba,
dado que ninguno tiene UI implementada todavía (todos son stubs de navegación de Sprint 1). Lo
que sí existe (`navigation.spec.ts`, `theme.spec.ts`) prueba infraestructura de shell, no
flujos de negocio.

## 3. Inventario de la suite unitaria/integración (contexto, no parte del alcance de esta Tarea)

```
Test Files  29 passed (29)
     Tests  188 passed (188)
```

Corrido con `npm run test` el mismo día, para tener el número real (no el citado de memoria en
mensajes de PR anteriores) al momento de este audit.

## 4. Conclusión y siguiente paso

- La suite E2E de infraestructura (nav/theme) está sana — no requiere trabajo de arreglo.
- El déficit real es la ausencia total de E2E de negocio, que no se puede cerrar en Sprint 2
  (la UI no existe) — se resuelve dejando el scaffold (Tarea 3-4 de este mismo sprint) para que
  cada epic de Sprint 3/4/6 lo complete al construir su UI, en vez de partir de cero.
